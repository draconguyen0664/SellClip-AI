server:
  port: 8083

spring:
  application:
    name: ai-service
  datasource:
    url: ${AI_DB_URL:jdbc:mysql://localhost:3306/sellclip_ai?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: root
    password: aptx4869
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
