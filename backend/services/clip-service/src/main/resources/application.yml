server:
  port: 8082

spring:
  application:
    name: clip-service
  datasource:
    url: ${CLIP_DB_URL:jdbc:mysql://localhost:3306/sellclip_clip?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
    username: root
    password: aptx4869
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    properties:
      hibernate:
        format_sql: true

management:
  endpoints:
    web:
      exposure:
        include: health,info
